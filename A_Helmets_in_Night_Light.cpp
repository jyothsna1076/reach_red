#include<bits/stdc++.h>
using namespace std;
int main()
{
    int tc;
    cin>>tc;
    while(tc--)
    {
        int n, p;
        cin>>n>>p;
        vector<int> maxpeople(n);
        vector<int> cost(n);
        for(auto &a: maxpeople) cin>>a;
        for(auto &a: cost) cin>>a;
        
    }
    return 0;
}